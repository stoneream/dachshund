---
title: artist-release-sync-queue
---

## 概要

各アーティストのリリース同期をキューイングする。

アーティストのリリースを取得する際には Spotify API を呼び出す必要があるが、何らかの原因により取得に失敗した場合は再試行できるようにする必要がある。
そのため、同期対象のキューイングと実際の同期処理を別ジョブに分割しており、同期処理は `artist-releases-sync` ジョブで行われる。

## 補足

リリース同期の対象は、有効なユーザーがフォローしているアーティストである。
